services:
  - type: web
    name: hrmanager4u-api
    env: node
    plan: free
    buildCommand: pnpm install && pnpm --filter api db:generate && pnpm --filter api build
    startCommand: pnpm --filter api db:migrate && pnpm --filter api db:seed && pnpm --filter api start:prod
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATABASE_URL
        fromDatabase:
          name: hrmanager4u-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true

databases:
  - name: hrmanager4u-db
    databaseName: hrmanager4u
    user: hrmanager4u_user
    plan: free
